# JS 2

Function `getTargetContent` defined in `main.js` has a lot of unnecessary code.
Refactor to the simplest possible form. If the searched element doesn't exist, function should return value which is logical `false` - `false`, `""`, `undefined`, `null` are accepted.
